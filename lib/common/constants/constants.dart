/// This class contains all the constants used in the app
class Constants {
  ///alpha value for the color
  static const alpha = 255;

  ///min values for red
  static const minRed = 0;

  /// max values for red
  static const maxRed = 255;

  /// min values for green
  static const minGreen = 0;

  /// max values for green
  static const maxGreen = 255;

  /// min values for blue
  static const minBlue = 0;

  /// max values for blue
  static const maxBlue = 255;

  /// min number of colors to gradient
  static const numColors = 2;
}
